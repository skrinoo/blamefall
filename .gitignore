# ─────────────────────────────────────────────────────────────
# 密钥 —— 本项目所有脚本只从环境变量读，这里只是兜底防线。
# 公开仓库的第一条要求是「无敏感信息泄露」，宁可多拦一层。
# ─────────────────────────────────────────────────────────────
.env
.env.*
*.pem
*.key
*.p12
credentials*
secrets*

# ↑ 上面那条 .env.* 会连模板一起吃掉，必须显式放回来。
# .env.example 里只有变量名和空值，不含任何真实密钥，是必须提交的部署说明。
!.env.example
!.env.local.example

# Vercel 本地开发目录（含 .env 的实际拷贝与构建缓存）
.vercel
node_modules

# ─────────────────────────────────────────────────────────────
# 批量生成产物
#
# 这两个文件由 scripts/generate-verdicts.ps1 生成，是**待人工挑选的中间产物**，
# 不是线上数据（线上数据是 data/verdicts.js，人工精修版，必须提交）。
#
# 默认忽略，理由：它们可以被任何人用自己的 key 完整复现，
# 且每次重跑都会产生大量 diff 噪音。
#
# 但如果你想把「AI 生成 → 人工精修」的过程作为提交材料里
# 「人机协作与方法」的证据，可以显式加进来：
#     git add -f data/verdicts.generated.js docs/generation-report.md
# ─────────────────────────────────────────────────────────────
data/verdicts.generated.js
docs/generation-report.md

# ─────────────────────────────────────────────────────────────
# IDE 与工作区标记（不属于项目本身）
# ─────────────────────────────────────────────────────────────
.aicoding-chat-workspace
.qoder/
.vscode/
.idea/
*.swp

# ─────────────────────────────────────────────────────────────
# 系统文件
# ─────────────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store
